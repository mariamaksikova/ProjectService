global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: project-service
    metrics_path: /metrics
    static_configs:
      # API на хосте (uvicorn --port 8000); на macOS/Windows в Docker Desktop
      - targets: ["host.docker.internal:8000"]
